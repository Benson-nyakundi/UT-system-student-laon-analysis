SELECT * 
FROM dbo.['UT Sys Student Loan DB$']

/* This is the cleaned original data I pulled from excel. To clean it I renamed each column to match what it actually represented,
eliminated unnecessary data, and arranged each row by university.